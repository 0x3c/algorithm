const lessThan = (x, y) => x < y;
const moreThan = (x, y) => x > y;
const lessEqThan = (x, y) => x <= y;
const moreEqThan = (x, y) => x >= y;

const easy = {
  lessThan,
  moreThan,
  lessEqThan,
  moreEqThan
};
module.exports = easy;
